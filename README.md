# 3ds-camera
This is pretty much a PoC for a camera app. To use, make a "camera" directory on the root of the SD. Press L or R to capture a photo, start to exit to the homebrew launcher.

The app writes the photos as PPM files, one for each front camera.
